# Screen: Cross-Asset Flow Map

**Route:** `/flows`
**Purpose:** Visualize money moving between asset classes over time

## Layout

- Heatmap (Plotly): x-axis = last 60 trading dates, y-axis = all buckets, color = R006 (dollar_vol_share_delta_20d). Red = outflow, green = inflow.
- Flow ranking table: current R007 values sorted descending with 20-day sparkline per bucket

## Open Questions

-
